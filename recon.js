function readyTestimonialsLink() {
  //optional
  //replace with function that can take out the ".html" at the end of the page name
}

function readyScroll() {
  $('#fullpage').fullpage({
    autoScrolling: true
  });
  //readyTestimonialsLink();
}

$(readyScroll);